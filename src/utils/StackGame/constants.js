//variables constantes del juego
export const BOX_HEIGHT = 0.5

export const hueColorInit = Math.floor(Math.random() * 360)

export const MODES = {
  init: 'init',
  move: 'move',
  fall: 'fall',
  gameover: 'gameover'
}
